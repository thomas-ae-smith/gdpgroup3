For all python libraries, in the library folder run:
python setup.py install --user
to install the python library in your home directory.
The --user flag may be omitted to install it globally.

Recommender:
numpy
pyml

Datastore:
mysql-connector
tvdb_api
